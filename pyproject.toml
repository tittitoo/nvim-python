[project]
name = "nvim-python"
version = "0.1.0"
description = "Default python for neovim"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "neovim>=0.3.1",
    "pynvim>=0.5.2",
]
